<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.cloudstorage.dao.CommentDAO">
	<sql id="columns">
		id, tip_id, user_id, text, likes, deleted, create_time
	</sql>

	<sql id="baseSelect">
		select
		<include refid="columns"/>
		from comment
	</sql>
</mapper>
